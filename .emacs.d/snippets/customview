# -*- mode: snippet -*-
# name: customview
# key: customview
# --
package ${1:`(mapconcat 'identity (cdr (member "src" (split-string default-directory "/" t))) ".")`};

import android.content.Context;
import android.util.AttributeSet;
import android.view.LayoutInflater;
import android.widget.LinearLayout;
import android.widget.TextView;
import butterknife.InjectView;
import butterknife.Views;
import ${2:`(find-package)`}.R;

public class ${3:`(file-name-sans-extension (buffer-name))`} extends LinearLayout {

    @InjectView(R.id.txt_title) TextView mTitleView;

    public $3(Context context, AttributeSet attrs) {
	super(context, attrs);
	LayoutInflater inflater = LayoutInflater.from(context);
	inflater.inflate(R.layout.${4:view_slot_result}, this, true);
	Views.inject(this);
    }

    public $3(Context context) {
	this(context, null);
    }

    $0
}